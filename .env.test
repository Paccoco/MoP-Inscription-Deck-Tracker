# Test Environment Configuration
NODE_ENV=test
PORT=5001

# Test Database (SQLite for speed and simplicity)
DB_TYPE=sqlite
DB_PATH=:memory:

# Test Security
JWT_SECRET=test-jwt-secret-not-for-production

# Disable external services during testing
DISCORD_WEBHOOK_URL=
GOTIFY_SERVER=
GOTIFY_TOKEN=

# Test logging
LOG_LEVEL=error

# Test admin
DEFAULT_ADMIN_USERNAME=testadmin
DEFAULT_ADMIN_PASSWORD=testpass123
